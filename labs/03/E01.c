#include<stdio.h>
int main() {
     int len;
     int brdth;
      int heig;
      len=10;
      brdth=10;
      heig=20;
      printf("The volume of the container is %d",len*brdth*heig);
return 0;
}
